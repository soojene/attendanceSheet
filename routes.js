//Global
const HOME = "/";
const ADD = "/addmember";
const SEARCH = "/search";
const SAVED = "/saved";
const LOGIN = "/login";
const LOGOUT = "/logout";
const JOIN = "/join";

//User
const USER = "/user";


const routes = {
    home:HOME,
    add:ADD,
    search:SEARCH,
    saved:SAVED,
    join:JOIN,
    login:LOGIN,
    logout:LOGOUT
};

export default routes;