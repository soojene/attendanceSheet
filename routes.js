//Global
const HOME = "/";
const JOIN = "/join";
const SEARCH = "/search";
const SAVED = "/saved";

//User
const USER = "/user";


const routes = {
    home:HOME,
    join:JOIN,
    search:SEARCH,
    saved:SAVED
};

export default routes;